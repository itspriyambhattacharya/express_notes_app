<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes App</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"
    />
  </head>
  <body class="bg-dark text-white">
    <main class="mt-5">
      <div class="container my-5">
        <h1 class="text-center mb-5">MyNotes</h1>
        <form
          class="bg-danger-subtle text-danger-emphasis p-5"
          style="border-radius: 15px"
          method="post"
          action="./index.html"
        >
          <div class="mb-3">
            <label for="noteHeading" class="form-label">Note Heading</label>
            <input
              type="text"
              class="form-control"
              id="noteHeading"
              name="noteHeading"
            />
          </div>
          <div class="mb-3">
            <label for="noteDescription" class="form-label"
              >Note Description</label
            >
            <textarea
              class="form-control"
              id="noteDescription"
              rows="5"
              name="noteDescription"
            ></textarea>
          </div>

          <button type="submit" class="btn btn-primary">Add Note</button>
        </form>
      </div>

      <div class="container my-5">
        <h3 class="text-center mb-5">Notes</h3>
      </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
